[project]
name = "backend"
version = "0.1.0"
description = "RAG Evaluation Pipeline Project Backend"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "supabase>=2.28.3",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.22",
]

[tool.uv]
managed = true
dev-dependencies = []