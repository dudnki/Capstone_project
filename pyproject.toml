[project]
name = "capstone-project"
version = "0.1.0"
description = "AI Convergence Capstone Project"
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
dependencies = [
    "supabase>=2.4.0",
    "python-dotenv>=1.0.1",
    "groq>=0.5.0",
]
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"